/**
 * SelfMotivation 字幕セグメントビルダーの thin wrapper。
 * 実装本体は @rekishi/renderer 側 (renderer/src/longform-captions.ts) にある（budoux 依存）。
 */
import { buildLongformCaptionSegments } from "@rekishi/renderer";

export { buildLongformCaptionSegments };
